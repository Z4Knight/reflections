初始化版本库时会发生什么情况？为什么你需要这样做？
答：会增加一个.git文件，这样做的目的是为了使用git进行版本控制。

暂存区与工作目录和版本库有何不同？你认为它具有什么价值？
答：暂存区只存放文件的快照，并不存放实际文件。价值：可以将逻辑上相同
的两个文件看做是一次commit。

如何使用暂存区确保为每项逻辑更改进行一次提交？
答：每次更改前可以使用 git diff 命令查看是否需要 commit。